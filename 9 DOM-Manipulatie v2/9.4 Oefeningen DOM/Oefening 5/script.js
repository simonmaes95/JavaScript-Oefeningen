let weekdays = document.querySelectorAll('.weekday-js');

weekdays.forEach(weekday => {
    weekday.style.fontSize = '30px';
    weekday.style.fontWeight = 'bold';
    weekday.style.border = '4px solid black'
    weekday.style.textAlign = 'center';
    weekday.style.padding = '5px';
});

